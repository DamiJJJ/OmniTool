services:

type: web
name: omnitool-app
env: python
buildCommand: "pip install -r requirements.txt"
startCommand: "bash run.sh"
envVars:

key: FLASK_ENV
value: production

key: DATABASE_URL
fromDatabase:
name: omnitool-db
property: connectionString

key: SECRET_KEY
generateValue: true